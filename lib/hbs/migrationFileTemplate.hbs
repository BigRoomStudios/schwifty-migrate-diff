'use strict';

const Items = require('items');

exports.up = function (knex, Promise) {

    // Table create

    return Promise.all([

    {{> build-table-create-list tableDeltas=migrations.tableDeltas tableType='tableDelta'}}

    {{> build-column-modify-list columnDeltas=migrations.columnDeltas}}

    ])
    .then(function() {

        // Join table create

        Promise.all([
        {{> build-table-create-list tableDeltas=migrations.tableDeltas tableType='joinTableDelta'}}
        ]);
    })
};

exports.down = function (knex, Promise) {

};
